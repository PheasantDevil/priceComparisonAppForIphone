{
  "name": "price-comparison-app",
  "version": "1.0.0",
  "description": "Price comparison application with Next.js frontend",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "frontend"
  ]
}
