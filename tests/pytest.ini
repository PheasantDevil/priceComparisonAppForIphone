[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -v -s --cov=src --cov-report=term-missing --cov-report=xml
markers =
    unit: Unit tests
    integration: Integration tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
